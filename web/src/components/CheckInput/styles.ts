import styled from "styled-components";

export const CheckInputContainer = styled.div`
   display: flex;
   align-items: center;
   flex-direction: row;
   flex: 1;

   img {
      height: 24px;
   }
`;
